api.url=${api.url}
device.id=${device.id}
platform=${platform}
driver.authorization=${driver.authorization}
driver.id=${driver.id}
broker.authorization=${broker.authorization}
broker.email=${broker.email}

os=${os}
browser.headless=${browser.headless}
ops.tool.authorization=${ops.tool.authorization}
ops.tool.host=${ops.tool.host}

db.host=${db.host}
db.name=${db.name}
db.user.name=${db.user.name}
db.password=${db.password}



